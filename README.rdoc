Many to many  Association:

1. We will implement using jhas_many through association
2. Requires a join table
3.One object/row in one table can br associated with multiple rows in 2nd table and vice-versa